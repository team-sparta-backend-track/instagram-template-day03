# Day 15: Auth Basics (Session/Cookie) - 인증의 기초

## 🎯 학습 목표
- HTTP의 무상태성(Stateless)과 이를 보완하기 위한 쿠키, 세션의 원리를 이해합니다.
- 서블릿이 제공하는 `HttpSession`을 사용하여 가장 기본적인 로그인을 구현합니다.
- 쿠키가 보안에 취약한 이유와 세션 하이재킹 개념을 배웁니다.

## 🛠️ 브랜치 준비 작업 (Pre-work)
*`day15` 브랜치 상태*
1. **AuthController**: 껍데기만 생성.
2. **사전 지식**: HTTP 요청 헤더에 Cookie가 어떻게 실리는지 보여주기 위해 Chrome 개발자 도구를 준비.

## 👨‍💻 라이브 세션 커밋 가이드 (Live Session Plan)

### 1️⃣ Commit 1: 로그인 API와 세션 생성
- **Task**: `POST /login`에서 아이디/비번이 맞으면 `request.getSession()`으로 세션을 만들고 key-value로 사용자 정보를 저장합니다.
- **설명**: `JSESSIONID`가 응답 헤더(Set-Cookie)로 내려가는 것을 확인합니다.

### 2️⃣ Commit 2: 로그인 체크 (Intercepter or Filter)
- **Task**: 로그인 안 한 사용자가 글을 쓰려고 할 때 막는 로직을 구현합니다. (아직 Spring Security 없이 순수 로직으로).
- **설명**: "모든 컨트롤러에 `if (session == null)`을 넣을 것인가?" -> 공통 처리(AOP/Filter/Interceptor)의 니즈 발견.

### 3️⃣ Commit 3: 로그아웃
- **Task**: `session.invalidate()`로 서버 쪽 세션을 파기합니다.
- **설명**: 브라우저의 쿠키를 지우는 것과 서버의 세션을 지우는 것의 차이.

## 💡 핵심 설명 포인트
- **Stateful Server vs Stateless Server**: 세션은 서버 메모리를 쓰므로 트래픽이 몰리면 서버가 터질 수 있다(스케일 아웃의 어려움). -> JWT 예고.
