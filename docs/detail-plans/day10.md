# Day 10: IoC/DI와 유연한 설계 (Interface)

## 🎯 학습 목표
- 스프링의 핵심인 IoC(제어의 역전)와 DI(의존성 주입)가 왜 필요한지 "변경"의 관점에서 이해합니다.
- 인터페이스를 통해 구현체를 갈아끼우는 전략(Strategy Pattern)을 실습합니다.

## 🛠️ 브랜치 준비 작업 (Pre-work)
*`day10` 브랜치 상태*
1. **강한 결합**: `PostService`가 `LocalFileSaver`라는 구체 클래스를 직접 `new` 하거나 필드로 박아놓은 상태로 만듭니다.

## 👨‍💻 라이브 세션 커밋 가이드 (Live Session Plan)

### 1️⃣ Commit 1: 문제 상황 인식 (요구사항 변경)
- **Task**: "이제 파일을 로컬이 아니라 AWS S3에 저장해야 합니다"라는 요구사항을 가정. `PostService` 코드를 뜯어고쳐야 함을 인지.
- **설명**: OCP(개방-폐쇄 원칙) 위반. 로직 변경을 위해 클라이언트 코드를 수정해야 한다.

### 2️⃣ Commit 2: 인터페이스 추출
- **Task**: `FileSaver` (혹은 `FileUploadUtil`) 인터페이스를 정의합니다. `saveFile()` 메소드를 선언합니다.
- **설명**: "무엇을(What) 할지만 정의하고, 어떻게(How) 할지는 구현체에 맡긴다".

### 3️⃣ Commit 3: 구현체 분리 및 DI 적용
- **Task**: `LocalFileSaver`와 `S3FileSaver`(Mock)가 인터페이스를 구현하게 하고, `PostService`는 인터페이스에 의존하게 바꿉니다.
- **코드**:
  ```java
  private final FileSaver fileSaver; // 인터페이스 의존
  ```

### 4️⃣ Commit 4: @Primary와 @Qualifier
- **Task**: 두 개의 빈(Bean)이 있을 때 스프링이 누구를 주입해야 할지 모르는 에러를 보고, `@Primary`나 프로파일(@Profile)로 갈아끼우는 시연을 합니다.

## 💡 핵심 설명 포인트
- **DIP(의존역전 원칙)**: 구체적인 것이 추상적인 것에 의존해야 한다.
- 스프링 컨테이너가 해주는 일: 프로그램 시작 시점에 딱 맞는 부품(Bean)을 조립해준다.
