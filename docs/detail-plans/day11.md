# Day 11: 팀 프로젝트 2 시작 (Payment) - 도메인 모델링

## 🎯 학습 목표
- 새로운 도메인(결제/주문 등)을 분석하고 모델링하는 능력을 기릅니다(인스타그램 클론의 경우 유료 멤버십이나 광고 결제 등으로 치환 가능).
- 팀원들과 DB 스키마를 논의하고 합의하는 과정을 겪습니다.

## 🛠️ 브랜치 준비 작업 (Pre-work)
*`day11` 브랜치 상태*
1. **요구사항 문서**: "사용자는 포인트를 충전할 수 있고, 포인트를 사용해 게시물 홍보를 할 수 있다"는 식의 가상 시나리오 준비.

## 👨‍💻 라이브 세션 커밋 가이드 (Live Session Plan)

### 1️⃣ Commit 1: 요구사항 분석 및 용어 정의 (Ubiquitous Language)
- **Task**: `Payment`, `Point`, `ChargeHistory` 등의 용어를 정의하고 `docs/domain_dictionary.md`(가칭) 같은 텍스트 파일에 정리해 커밋합니다.
- **설명**: 개발자끼리, 그리고 기획자와 같은 용어를 쓰는 것이 DDD의 시작.

### 2️⃣ Commit 2: ERD 설계
- **Task**: 칠판(혹은 draw.io)을 사용해 테이블 관계를 그립니다. `Payment`는 `Member`와 1:N 이고, `Post`와도 연관될 수 있음을 논의.

### 3️⃣ Commit 3: Entity 클래스 스켈레톤 작성
- **Task**: 논의된 내용을 바탕으로 엔티티 클래스들의 껍데기를 만들고 필드를 정의해 커밋합니다.

### 4️⃣ Commit 4: Repository 설계
- **Task**: 어떤 쿼리가 필요할지 예상해보고 Repository 인터페이스에 메소드 명을 적어봅니다.

## 💡 핵심 설명 포인트
- 코딩보다 설계가 먼저다. 설계가 엉망이면 코드는 스파게티가 된다.
- **정규화 vs 반정규화**: 성능을 위해 중복 데이터를 허용할 것인가? (결제 내역은 불변해야 하므로 별도 테이블이 낫다 등).
