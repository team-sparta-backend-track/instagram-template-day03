# Day 24: Load Testing (k6) - 부하 테스트와 튜닝

## 🎯 학습 목표
- "내 서버는 몇 명까지 버틸까?"를 알기 위해 스트레스 테스트 도구(k6)를 사용해봅니다.
- TPS(Transactions Per Second)와 Latency(응답 속도)의 관계를 파악합니다.
- 적절한 병목 지점(DB 인덱스, 로직)을 찾아 개선합니다.

## 🛠️ 브랜치 준비 작업 (Pre-work)
*`day24` 브랜치 상태*
1. **k6 설치**: 로컬에 k6 도구 설치 안내.
2. **데이터 뻥튀기**: DB에 수만 건의 데이터를 넣어둬야 유의미한 테스트가 됨.

## 👨‍💻 라이브 세션 커밋 가이드 (Live Session Plan)

### 1️⃣ Commit 1: 시나리오 스크립트 작성 (JS)
- **Task**: `k6-script.js`를 작성합니다. 가상 유저(VU)가 메인 피드를 조회하고 게시물을 클릭하는 시나리오를 코딩합니다.
- **설명**: Ramp-up(서서히 사용자 증가) 설정.

### 2️⃣ Commit 2: 1차 테스트 및 모니터링
- **Task**: 테스트를 돌리고 터미널 결과창을 분석합니다. (예: 평균 응답 2초, 에러율 5%).

### 3️⃣ Commit 3: 병목 분석 (Slow Query)
- **Task**: DB Slow Query 로그를 확인하거나 `EXPLAIN`으로 쿼리 실행 계획을 봅니다. 인덱스가 없어서 Full Scan 하는 곳을 찾습니다.

### 4️⃣ Commit 4: 인덱스 적용 및 2차 테스트
- **Task**: `CREATE INDEX` 후 다시 테스트를 돌려 TPS가 얼마나 드라마틱하게 오르는지 확인합니다.

## 💡 핵심 설명 포인트
- **Premature Optimization**: 미리 최적화하지 마라. 측정(Measure)하고 고쳐라.
- **Saturation Point**: 시스템이 임계점을 넘으면 성능이 급격히 저하되는 지점.
